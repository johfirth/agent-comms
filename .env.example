# Database connection — change password for production!
DATABASE_URL=postgresql+asyncpg://agent_comms:agent_comms_dev@localhost:5432/agent_comms

# Admin API key — MUST be changed before deploying to production
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
ADMIN_API_KEY=admin-dev-key-change-me
